image: microsoft/dotnet:latest

stages:
  - build
  - test

variables:
  project: "NewTypes"

before_script:
  - "dotnet restore"

build:
  stage: build
  script:
    - "dotnet build"

test:
  stage: test
  script:
    - "dotnet test"
